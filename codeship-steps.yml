- name: Registry
  type: serial
  service: app
  encrypted_dockercfg_path: codeship-dockercfg.encrypted
  steps:
  - name: Push master builds
    type: push
    image_name: wincus/ntop
    image_tag: dev
